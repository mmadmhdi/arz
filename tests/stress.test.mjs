import assert from 'node:assert/strict';import {buildForecast} from '../netlify/functions/_lib/forecast-engine.mjs';
function series(mode){let p=180000,a=[];for(let i=0;i<220;i++){let r=0;if(mode==='trend')r=.0015+.001*Math.sin(i/8);if(mode==='range')r=.004*Math.sin(i/4);if(mode==='shock')r=(i===218?.08:.0004+.002*Math.sin(i));if(mode==='volatile')r=.012*Math.sin(i*1.7);p*=Math.exp(r);a.push({date:new Date(Date.UTC(2025,11,1+i)).toISOString().slice(0,10),close:p})}return a}
for(const mode of ['trend','range','shock','volatile']){const h=series(mode),current=h.at(-1).close,f=buildForecast({history:h,currentMarket:{mid:current,asOf:h.at(-1).date,disagreementPct:.005,quality:'high'},usdt:{mid:current*1.002,disagreementPct:.006,spreadPct:.001},news:{score:0,relevantCount:0},macro:{score:0},now:new Date('2026-07-10T15:00:00Z')});assert.ok(Number.isFinite(f.forecast));assert.ok(Math.abs(f.forecast/current-1)<=.061)}console.log('stress ok');
