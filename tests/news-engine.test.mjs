import assert from 'node:assert/strict';import {dedupeArticles,scoreNews} from '../netlify/functions/_lib/news-engine.mjs';
const now=new Date('2026-09-01T08:00:00Z'),arts=[{title:'U.S. imposes new sanctions on Iran oil network',domain:'reuters.com',publishedAt:'2026-09-01T06:00:00Z',url:'a'},{title:'US imposes new sanctions on Iran oil network',domain:'example.com',publishedAt:'2026-09-01T06:05:00Z',url:'b'},{title:'بانک مرکزی از تزریق ارز به بازار خبر داد',domain:'irna.ir',publishedAt:'2026-09-01T07:00:00Z',url:'c'},{title:'آتش بس و کاهش تنش اعلام شد',domain:'bbc.com',publishedAt:'2026-09-01T07:15:00Z',url:'d'}];
assert.ok(dedupeArticles(arts).length<arts.length);const s=scoreNews(arts,now);assert.ok(Number.isFinite(s.score));assert.ok(s.relevantCount>=3);assert.ok(s.score>-1&&s.score<1);const cut=scoreNews([{title:'بانک مرکزی کاهش عرضه ارز به بازار را اعلام کرد',domain:'cbi.ir',publishedAt:'2026-09-01T07:40:00Z'}],now);assert.ok(cut.score>0);const inject=scoreNews([{title:'بانک مرکزی آمادگی تزریق و عرضه ارز به بازار را اعلام کرد',domain:'cbi.ir',publishedAt:'2026-09-01T07:40:00Z'}],now);assert.ok(inject.score<0);console.log('news-engine ok',s.score,s.relevantCount);
